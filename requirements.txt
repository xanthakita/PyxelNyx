
# Core dependencies for Human Blur CLI Tool
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Optional: For GPU acceleration (if CUDA available)
# Install separately based on your CUDA version:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
